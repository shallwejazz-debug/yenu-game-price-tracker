{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run check:static",
    "check:static": "node --check public/static/admin.js && node --check public/static/watcher-admin.js && node --check public/static/app.js"
    "check": "npm run build && npm run check:static",
    "preview": "wrangler pages dev",
    "deploy": "npm run check && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.26"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.25.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
